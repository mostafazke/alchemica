---
plan: 10-02
phase: 10-streak-bonus
wave: 1-2
status: ready
autonomous: true
requirements: [STRK-05, STRK-06]
---

# Plan 10-02: Streak Bonus — Implementation

## Goal

Make the combo cap reactive to `streakCount` and show a 🔥 indicator in the TopBar when streak bonus is active. All 5 tests GREEN.

## Tasks

### Wave 1: reactions.ts — dynamic combo cap

- [ ] In `src/lib/game/reactions.ts`, add import: `import { streakCount } from '../stores/achievements.js';`
- [ ] Replace the hardcoded `Math.min(currentCombo + 1, 8)` with:
  ```ts
  const cap = Math.min(8 + get(streakCount), 11);
  const newCombo = wasSuccess ? Math.min(currentCombo + 1, cap) : 1;
  ```
- [ ] Run tests — confirm 5/5 GREEN

### Wave 2: TopBar.svelte — flame indicator

- [ ] In `src/lib/components/TopBar.svelte` `<script>`, add import:
  ```ts
  import { streakCount } from '../stores/achievements.js';
  ```
- [ ] Update combo span:
  ```svelte
  <span class="stat combo" class:pulse={$combo > 1} class:streak={$streakCount >= 1}>
    x{$combo}{$streakCount >= 1 ? ' 🔥' : ''}
  </span>
  ```
- [ ] Add CSS in `<style>`:
  ```css
  .stat.combo.streak {
    color: #ff8c42;
    text-shadow: 0 0 8px #ff8c4260;
  }
  ```

### Wave 3: Build + regression verification

- [ ] `npm run build` exits 0
- [ ] `npm run test:unit -- --run` — all tests GREEN (no regressions)

## Verification

- [ ] `streakCount=0` → combo cap 8, no 🔥, yellow color unchanged
- [ ] `streakCount=1` → combo cap 9, 🔥 visible, orange glow on TopBar combo
- [ ] `npm run build` exits 0
- [ ] All unit tests GREEN
