---
phase: 02-mobile-layout
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/app.html
  - src/lib/components/ElementCard.svelte
  - src/lib/components/Slot.svelte
  - src/lib/components/MixingChamber.svelte
  - src/lib/components/Shelf.svelte
autonomous: true
requirements: [LAYT-01, TOUC-04]
must_haves:
  truths:
    - "src/app.html has touch-action meta or CSS prevents double-tap zoom"
    - "All interactive elements have touch-action: manipulation"
    - "No double-tap zoom on element cards or slots"
    - "Viewport meta tag has user-scalable=no or equivalent"
  artifacts:
    - src/app.html
  key_links:
    - "app.html viewport meta includes maximum-scale=1"
---

<objective>
Add `touch-action: manipulation` to all interactive elements to prevent double-tap zoom.
Update viewport meta tag. Run a final mobile check.

Purpose: Prevents the 300ms tap delay and double-tap zoom on mobile Safari.
Output: All interactive components have touch-action: manipulation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update viewport meta tag in app.html</name>
  <files>src/app.html</files>
  <read_first>src/app.html</read_first>
  <action>
    Read src/app.html. Update the viewport meta tag to prevent user scaling:
    Change: `content="width=device-width, initial-scale=1"`
    To: `content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover"`

    This prevents double-tap zoom on iOS Safari. The viewport-fit=cover ensures content
    extends to screen edges on notched devices.

    Also verify these meta tags are present (add if missing):
    - `<meta name="theme-color" content="#0d1b2e">`
    - `<meta name="mobile-web-app-capable" content="yes">`
    - `<meta name="apple-mobile-web-app-capable" content="yes">`
    - `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">`
  </action>
  <verify>grep -q 'maximum-scale=1' src/app.html</verify>
  <done>app.html has viewport meta preventing double-tap zoom</done>
</task>

<task type="auto">
  <name>Task 2: Add touch-action: manipulation to ElementCard</name>
  <files>src/lib/components/ElementCard.svelte</files>
  <read_first>src/lib/components/ElementCard.svelte</read_first>
  <action>
    Add `touch-action: manipulation` to `.element-card` selector in ElementCard.svelte.
    This eliminates the 300ms tap delay and prevents double-tap zoom on this element.
  </action>
  <verify>grep -q 'touch-action' src/lib/components/ElementCard.svelte</verify>
  <done>ElementCard has touch-action: manipulation</done>
</task>

<task type="auto">
  <name>Task 3: Add touch-action to Slot, MixingChamber buttons, Shelf tabs</name>
  <files>src/lib/components/Slot.svelte, src/lib/components/MixingChamber.svelte, src/lib/components/Shelf.svelte</files>
  <read_first>src/lib/components/Slot.svelte, src/lib/components/MixingChamber.svelte, src/lib/components/Shelf.svelte</read_first>
  <action>
    For each file, add `touch-action: manipulation` to the primary interactive element:
    - Slot.svelte: add to the slot button/div
    - MixingChamber.svelte: add to `.react-btn`
    - Shelf.svelte: add to `.tab-btn` and `.category-header`

    In app.css, also add a global rule to catch all buttons:
    ```css
    button, [role="button"] {
      touch-action: manipulation;
    }
    ```
  </action>
  <verify>grep -q 'touch-action' src/lib/components/MixingChamber.svelte</verify>
  <done>All interactive elements have touch-action: manipulation</done>
</task>

<task type="auto">
  <name>Task 4: Build verification — npm run check</name>
  <files></files>
  <action>
    Run `npm run check` from /c/mostafa/front_end/projects/alchemica to verify TypeScript
    and Svelte compilation with no errors.
    Fix any TypeScript errors found.
  </action>
  <verify>npm run check exits with code 0</verify>
  <done>No TypeScript or Svelte errors after all Phase 2 changes</done>
</task>

</tasks>
