---
phase: 02-mobile-layout
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/components/Shelf.svelte
autonomous: true
requirements: [LAYT-05, GAME-02]
must_haves:
  truths:
    - "Elements in the shelf are grouped by category with section headers"
    - "Each category group can be collapsed/expanded"
    - "Section headers show category name and element count"
    - "Basic elements category is always expanded by default"
  artifacts:
    - src/lib/components/Shelf.svelte
  key_links:
    - "Shelf uses ELEMENTS data to group by category field"
---

<objective>
Implement category-grouped shelf with visible section headers and collapsible groups.
Elements are organized by their category field (basic, fire, water, earth, air, metal, energy, gas, compound).
Each group shows a header with category name + count and can be collapsed.

Purpose: Makes finding elements easier on mobile with many unlocked elements.
Output: Updated Shelf.svelte with grouped/collapsible category display.
</objective>

<context>
Current Shelf.svelte shows flat list of elements with filter tabs (All/Basic/Found).
Need to add a "By Category" view or modify the existing view to group when showing "All".
Category data is in ELEMENTS[key].category.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add category grouping logic to Shelf.svelte</name>
  <files>src/lib/components/Shelf.svelte</files>
  <read_first>src/lib/components/Shelf.svelte, src/lib/data/elements.ts, src/lib/types.ts</read_first>
  <action>
    Update Shelf.svelte to group elements by category. When filter is 'all', group by category
    with collapsible sections. When filter is 'basic' or 'found', show flat list (no grouping needed).

    Add category icons map:
    ```ts
    const CATEGORY_META: Record<string, { label: string; icon: string }> = {
      basic:    { label: 'Basic',    icon: '⬡' },
      fire:     { label: 'Fire',     icon: '🔥' },
      water:    { label: 'Water',    icon: '💧' },
      earth:    { label: 'Earth',    icon: '🪨' },
      air:      { label: 'Air',      icon: '🌬' },
      metal:    { label: 'Metal',    icon: '⚙️' },
      energy:   { label: 'Energy',   icon: '⚡' },
      gas:      { label: 'Gas',      icon: '💨' },
      compound: { label: 'Compound', icon: '🧪' },
    };
    ```

    Add collapsed state as a Set:
    ```ts
    let collapsedCategories = $state(new Set<string>());
    function toggleCategory(cat: string) {
      collapsedCategories = new Set(
        collapsedCategories.has(cat)
          ? [...collapsedCategories].filter(c => c !== collapsedCategories.has(c) ? true : c !== cat)
          : [...collapsedCategories, cat]
      );
    }
    ```

    Actually use this simpler approach:
    ```ts
    let collapsedCategories: Set<string> = $state(new Set());
    function toggleCategory(cat: string) {
      const next = new Set(collapsedCategories);
      if (next.has(cat)) next.delete(cat); else next.add(cat);
      collapsedCategories = next;
    }
    ```

    Add grouped derived:
    ```ts
    const CATEGORY_ORDER = ['basic', 'fire', 'water', 'earth', 'air', 'metal', 'energy', 'gas', 'compound'];

    const groupedElements = $derived(() => {
      if (filter !== 'all') return null;
      const groups = new Map<string, string[]>();
      for (const key of [...$unlockedElements]) {
        const cat = ELEMENTS[key]?.category ?? 'compound';
        if (!groups.has(cat)) groups.set(cat, []);
        groups.get(cat)!.push(key);
      }
      return CATEGORY_ORDER.filter(cat => groups.has(cat)).map(cat => ({
        cat,
        keys: groups.get(cat)!,
        meta: CATEGORY_META[cat] ?? { label: cat, icon: '⬡' }
      }));
    });
    ```

    Update the template to render grouped view when filter === 'all':
    ```svelte
    <div class="elements-list">
      {#if filter === 'all' && groupedElements}
        {#each groupedElements as group (group.cat)}
          <div class="category-group">
            <button
              class="category-header"
              onclick={() => toggleCategory(group.cat)}
            >
              <span class="cat-icon">{group.meta.icon}</span>
              <span class="cat-label">{group.meta.label}</span>
              <span class="cat-count">{group.keys.length}</span>
              <span class="cat-chevron" class:collapsed={collapsedCategories.has(group.cat)}>▾</span>
            </button>
            {#if !collapsedCategories.has(group.cat)}
              {#each group.keys as key (key)}
                <ElementCard elementKey={key} />
              {/each}
            {/if}
          </div>
        {/each}
      {:else}
        {#each filteredKeys as key (key)}
          <ElementCard elementKey={key} />
        {:else}
          <p class="empty-shelf">None yet — combine basic elements!</p>
        {/each}
      {/if}
    </div>
    ```

    Add category group styles:
    ```css
    .category-group { margin-bottom: 4px; }
    .category-header {
      display: flex; align-items: center; gap: 6px;
      width: 100%; padding: 6px 4px;
      background: transparent; border: none; border-bottom: 1px solid #1a2e4a;
      color: #4a6080; font-size: 10px; text-transform: uppercase; letter-spacing: 0.5px;
      cursor: pointer; text-align: left;
      min-height: 36px;
    }
    .category-header:hover { color: #8ab4d4; }
    .cat-icon { font-size: 12px; }
    .cat-label { flex: 1; font-family: 'Space Mono', monospace; }
    .cat-count {
      background: #1a2e4a; color: #4af0c0;
      font-size: 9px; padding: 1px 5px; border-radius: 8px;
      font-family: 'Space Mono', monospace;
    }
    .cat-chevron { font-size: 10px; transition: transform 0.2s; }
    .cat-chevron.collapsed { transform: rotate(-90deg); }
    ```
  </action>
  <verify>grep -q 'category-header' src/lib/components/Shelf.svelte</verify>
  <done>Shelf.svelte shows elements grouped by category with collapsible sections</done>
</task>

</tasks>
