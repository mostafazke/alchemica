---
phase: 06-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
autonomous: true
requirements:
  - ACHV-05
  - ACHV-06

must_haves:
  truths:
    - "AchievementId type exists and is a string union of the 4 badge keys"
    - "All badge IDs are captured at the type level so TypeScript catches typos in every downstream consumer"
    - "The v2 save shape interface is defined and exported from types.ts so game.ts and storage.ts import from one source of truth"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "AchievementId union type + SaveDataV2 interface"
      exports: ["AchievementId", "SaveDataV2"]
  key_links:
    - from: "src/lib/stores/achievements.ts (Plan 02)"
      to: "src/lib/types.ts"
      via: "import type { AchievementId }"
      pattern: "AchievementId"
    - from: "src/lib/stores/game.ts (Plan 03)"
      to: "src/lib/types.ts"
      via: "import type { SaveDataV2 }"
      pattern: "SaveDataV2"
---

<objective>
Add `AchievementId` union type and `SaveDataV2` interface to `src/lib/types.ts`.

Purpose: Establish the TypeScript contracts that every Phase 6 plan depends on. Plans 02 and 03 import from this file — it must be created first.
Output: Updated `src/lib/types.ts` with two new exports: `AchievementId` and `SaveDataV2`.
</objective>

<execution_context>
@C:/Users/mosta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mosta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-foundation/06-CONTEXT.md

<interfaces>
<!-- Current src/lib/types.ts — executor reads this file before editing -->
Current exports:
  - Category (union type)
  - Element (interface)
  - Discovery (interface)
  - GameState (interface)
  - Slots (interface)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AchievementId type and SaveDataV2 interface to types.ts</name>
  <files>src/lib/types.ts</files>

  <read_first>
    - src/lib/types.ts — read the full current file before editing; append after the existing Slots interface
  </read_first>

  <action>
Append two new exports to `src/lib/types.ts` after the existing `Slots` interface. Do NOT modify existing exports.

**Add exactly:**

```ts
/** Per D-14: Union of all achievement badge IDs. Add new IDs here when new badges are introduced. */
export type AchievementId = 'badge_10' | 'badge_25' | 'badge_50' | 'badge_61';

/**
 * V2 save data shape stored under the 'alchemica_v1' localStorage key.
 * Per D-01 (same key, version bump to 2), D-03 (streak fields), D-05 (earnedAchievements field).
 */
export interface SaveDataV2 {
  version: 2;
  data: {
    unlockedElements: string[];
    discoveries: Discovery[];
    score: number;
    earnedAchievements: string[];   // serialized as array; reconstructed as Set<AchievementId> on load (D-13)
    streakCount: number;            // per D-03
    lastCompletedDate: string | null; // ISO date YYYY-MM-DD, per D-03
  };
}
```

No other lines in types.ts are changed.
  </action>

  <verify>
    <automated>npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `src/lib/types.ts` contains `export type AchievementId = 'badge_10' | 'badge_25' | 'badge_50' | 'badge_61'`
    - `src/lib/types.ts` contains `export interface SaveDataV2`
    - `src/lib/types.ts` contains `version: 2;` inside SaveDataV2
    - `src/lib/types.ts` contains `earnedAchievements: string[];`
    - `src/lib/types.ts` contains `streakCount: number;`
    - `src/lib/types.ts` contains `lastCompletedDate: string | null;`
    - `npm run check` exits 0 (no TypeScript errors)
  </acceptance_criteria>

  <done>types.ts exports AchievementId and SaveDataV2; svelte-check passes with 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| types.ts → all consumers | Type definitions are compile-time only; no runtime trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | AchievementId union | accept | Type-only file; no runtime execution path; TypeScript enforces valid values at compile time |
</threat_model>

<verification>
Run `npm run check` from the project root. Expect 0 errors and 0 warnings.

Grep checks:
- `grep -c "AchievementId" src/lib/types.ts` → must be ≥ 1
- `grep -c "SaveDataV2" src/lib/types.ts` → must be ≥ 1
- `grep -c "badge_10" src/lib/types.ts` → must be ≥ 1
- `grep -c "earnedAchievements" src/lib/types.ts` → must be ≥ 1
</verification>

<success_criteria>
- AchievementId type union with 4 badge keys exported from types.ts
- SaveDataV2 interface with all v2 fields exported from types.ts
- 0 TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-foundation/06-01-SUMMARY.md`
</output>
