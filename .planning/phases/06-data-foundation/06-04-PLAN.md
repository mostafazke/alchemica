---
phase: 06-data-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - src/lib/components/TopBar.svelte
autonomous: true
requirements:
  - PROG-01

must_haves:
  truths:
    - "TopBar shows discovery count as '{count}/{total} discovered' at all times"
    - "The microscope emoji (🔬) is removed from the discovery counter span"
    - "Total element count remains dynamic (Object.keys(ELEMENTS).length), not hardcoded as 61"
    - "No new CSS classes, no new styles, no layout changes in this plan"
    - "Color of the discovery stat remains #8ab4d4 (muted text — not accent)"
  artifacts:
    - path: "src/lib/components/TopBar.svelte"
      provides: "Updated discovery counter span text"
      contains: "discovered"
  key_links:
    - from: "TopBar.svelte discovery span"
      to: "$unlockedElements store"
      via: "$unlockedElements.size directly in template"
      pattern: "unlockedElements.size"
---

<objective>
Update the discovery counter span in `TopBar.svelte` to display `{count}/{total} discovered` instead of `🔬 {count}/{total}`.

Purpose: Satisfies PROG-01 and D-09/D-10/D-11. This is a text-only change — no layout, no styling, no new stores.
Output: Updated `src/lib/components/TopBar.svelte` line 10.
</objective>

<execution_context>
@C:/Users/mosta/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mosta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-data-foundation/06-CONTEXT.md
@.planning/phases/06-data-foundation/06-UI-SPEC.md

<interfaces>
<!-- Exact before/after from UI-SPEC.md -->
Before (current line 10 in TopBar.svelte):
```svelte
<span class="stat">🔬 {$unlockedElements.size}/{Object.keys(ELEMENTS).length}</span>
```

After (required by D-09, PROG-01, UI-SPEC.md):
```svelte
<span class="stat">{$unlockedElements.size}/{Object.keys(ELEMENTS).length} discovered</span>
```

Design constraints from UI-SPEC.md:
- CSS class: `.stat` — unchanged
- Color: #8ab4d4 (muted text) — NOT changed to accent color #4af0c0
- Font: Space Mono 12px weight 400 — unchanged
- No new @keyframes, no .pulse modifier in Phase 6 (deferred to Phase 8 per D-12)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace discovery counter text in TopBar.svelte</name>
  <files>src/lib/components/TopBar.svelte</files>

  <read_first>
    - src/lib/components/TopBar.svelte — read the full current file before editing; confirm line 10 is the stat span to change
  </read_first>

  <action>
In `src/lib/components/TopBar.svelte`, replace the single discovery counter span on line 10.

**Find:**
```svelte
<span class="stat">🔬 {$unlockedElements.size}/{Object.keys(ELEMENTS).length}</span>
```

**Replace with:**
```svelte
<span class="stat">{$unlockedElements.size}/{Object.keys(ELEMENTS).length} discovered</span>
```

No other lines are changed. The `<script>` block, all other spans, the `<style>` block, and every CSS rule remain identical to the current file.

Do NOT:
- Add a new CSS class to this span
- Change the `.stat` CSS rule color from `#8ab4d4`
- Add a derived store for discovery count (deferred to Phase 8 per D-11)
- Add a pulse animation (deferred to Phase 8 per D-12)
- Hardcode the total as `61` (must remain `Object.keys(ELEMENTS).length` per D-10)
  </action>

  <verify>
    <automated>npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "discovered" src/lib/components/TopBar.svelte` returns 1
    - `grep "🔬" src/lib/components/TopBar.svelte` returns no matches (emoji removed)
    - `grep -c "unlockedElements.size" src/lib/components/TopBar.svelte` returns 1 (dynamic count, not hardcoded)
    - `grep -c "Object.keys(ELEMENTS).length" src/lib/components/TopBar.svelte` returns 1 (dynamic total per D-10)
    - `grep "hardcoded\|61 \|= 61" src/lib/components/TopBar.svelte` returns no matches (no hardcoded total)
    - `grep -c "#8ab4d4" src/lib/components/TopBar.svelte` returns 1 (muted text color unchanged)
    - `npm run check` exits 0
  </acceptance_criteria>

  <done>TopBar discovery counter shows "{count}/{total} discovered" with no emoji; dynamic total; no style changes; svelte-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TopBar display | Read-only display of store value; no user input accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | TopBar element count display | accept | Displaying count of discovered elements is intended game feature; no sensitive data exposed |
</threat_model>

<verification>
Run `npm run check` — expect 0 errors.

Visual verification (optional — no checkpoint required):
- Run `npm run dev`, open the app
- TopBar should display e.g. "4/61 discovered" on fresh start
- After unlocking an element, count should update reactively
- No microscope emoji visible in the stat
</verification>

<success_criteria>
- TopBar discovery counter reads "{$unlockedElements.size}/{Object.keys(ELEMENTS).length} discovered"
- 🔬 emoji removed
- No hardcoded total
- No style changes
- 0 TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-foundation/06-04-SUMMARY.md`
</output>
