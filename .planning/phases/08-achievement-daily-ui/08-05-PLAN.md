---
phase: 08-achievement-daily-ui
plan: 05
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/components/TopBar.svelte
  - src/lib/components/BottomBar.svelte
autonomous: true
requirements:
  - PROG-02
  - PROG-03

must_haves:
  truths:
    - "TopBar discovery stat gets class 'badge-pulse' for ~800ms when earnedAchievements size increases"
    - "TopBar pulse animation: scale 1→1.25→1 + brief gold color transition"
    - "BottomBar has 4 buttons: Elements | Discoveries | Achievements | Settings"
    - "Achievements button uses 🏆 emoji, calls onToggleAchievements prop"
    - "BottomBar .bottom-stat element is removed (count already in TopBar)"
    - "BottomBar accepts achievementsOpen and onToggleAchievements props"
  artifacts:
    - path: "src/lib/components/TopBar.svelte"
      provides: "TopBar with badge-pulse animation on earnedAchievements milestone"
    - path: "src/lib/components/BottomBar.svelte"
      provides: "BottomBar with 4th Achievements button"
  key_links:
    - from: "src/routes/+page.svelte (Plan 07)"
      to: "src/lib/components/BottomBar.svelte"
      via: "achievementsOpen + onToggleAchievements props"
---

<objective>
Modify `TopBar.svelte` to add badge-pulse animation on milestone unlock.
Modify `BottomBar.svelte` to add Achievements button and remove the redundant stat text.

**TopBar change:** Watch `earnedAchievements` store. When its size increases, apply CSS class `badge-pulse` to the discovery counter span for 800ms, then remove it. Use `$effect` + a local `pulseActive` boolean state.

**BottomBar change:** 
1. Add props: `achievementsOpen: boolean`, `onToggleAchievements: () => void`
2. Remove `<span class="bottom-stat">` element
3. Add 🏆 Achievements button between Discoveries and Settings
4. Remove `.bottom-stat` CSS rule (no longer needed)
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Modify src/lib/components/TopBar.svelte</name>
  <files>src/lib/components/TopBar.svelte</files>
  <action>
Replace the entire file with this content:

```svelte
<script lang="ts">
  import { score, combo, unlockedElements } from '../stores/game.js';
  import { resetGame } from '../stores/game.js';
  import { earnedAchievements } from '../stores/achievements.js';
  import { ELEMENTS } from '../data/elements.js';

  let pulseActive = $state(false);
  let prevEarnedSize = earnedAchievements ? 0 : 0;

  $effect(() => {
    const size = $earnedAchievements.size;
    if (size > prevEarnedSize && prevEarnedSize >= 0) {
      pulseActive = true;
      setTimeout(() => { pulseActive = false; }, 800);
    }
    prevEarnedSize = size;
  });
</script>

<header class="top-bar">
  <div class="top-bar-title">⚗️ Alchemica</div>
  <div class="top-bar-stats">
    <span class="stat" class:badge-pulse={pulseActive}>{$unlockedElements.size}/{Object.keys(ELEMENTS).length} discovered</span>
    <span class="stat combo" class:pulse={$combo > 1}>x{$combo}</span>
    <span class="stat score">{$score}</span>
    <button class="reset-btn" onclick={resetGame} title="Reset game">↺</button>
  </div>
</header>

<style>
  .top-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: env(safe-area-inset-top, 0px) max(16px, env(safe-area-inset-right, 16px)) 0 max(16px, env(safe-area-inset-left, 16px));
    min-height: calc(52px + env(safe-area-inset-top, 0px));
    background: #0d1b2e;
    border-bottom: 1px solid #1a2e4a;
    flex-shrink: 0;
    z-index: 10;
  }
  .top-bar-title {
    font-family: 'Space Mono', monospace;
    font-size: 16px;
    color: #4af0c0;
    font-weight: 700;
    letter-spacing: 1px;
  }
  .top-bar-stats {
    display: flex;
    gap: 12px;
    align-items: center;
  }
  .stat {
    font-family: 'Space Mono', monospace;
    font-size: 12px;
    color: #8ab4d4;
    transition: color 0.3s;
  }
  .stat.combo {
    color: #ffe44a;
    font-size: 15px;
    font-weight: 700;
    transition: transform 0.1s;
  }
  .stat.score { color: #4af0c0; }
  .stat.badge-pulse {
    animation: badge-pulse 0.8s ease;
  }
  @keyframes badge-pulse {
    0%   { transform: scale(1);    color: #8ab4d4; }
    30%  { transform: scale(1.25); color: #e8b84b; }
    60%  { transform: scale(1.1);  color: #e8b84b; }
    100% { transform: scale(1);    color: #8ab4d4; }
  }
  .reset-btn {
    background: transparent;
    border: 1px solid #1a3a5a;
    border-radius: 6px;
    color: #4a6080;
    font-size: 16px;
    cursor: pointer;
    padding: 2px 8px;
    min-height: 28px;
    touch-action: manipulation;
    transition: color 0.15s;
  }
  .reset-btn:hover { color: #ff6b6b; }
</style>
```
  </action>
</task>

<task type="auto">
  <name>Task 2: Modify src/lib/components/BottomBar.svelte</name>
  <files>src/lib/components/BottomBar.svelte</files>
  <action>
Replace the entire file with this content:

```svelte
<script lang="ts">
  let {
    shelfOpen = false,
    onToggleShelf,
    discoverySheetOpen = false,
    onToggleDiscoveries,
    achievementsOpen = false,
    onToggleAchievements,
    settingsOpen = false,
    onToggleSettings
  }: {
    shelfOpen?: boolean;
    onToggleShelf?: () => void;
    discoverySheetOpen?: boolean;
    onToggleDiscoveries?: () => void;
    achievementsOpen?: boolean;
    onToggleAchievements?: () => void;
    settingsOpen?: boolean;
    onToggleSettings?: () => void;
  } = $props();
</script>

<footer class="bottom-bar">
  <button
    class="bar-btn"
    class:active={shelfOpen}
    onclick={onToggleShelf}
    aria-label="Toggle elements shelf"
  >
    🧪 <span class="bar-btn-label">Elements</span>
  </button>
  <button
    class="bar-btn"
    class:active={discoverySheetOpen}
    onclick={onToggleDiscoveries}
    aria-label="Toggle discoveries"
  >
    📋 <span class="bar-btn-label">Discoveries</span>
  </button>
  <button
    class="bar-btn"
    class:active={achievementsOpen}
    onclick={onToggleAchievements}
    aria-label="Open achievements"
  >
    🏆 <span class="bar-btn-label">Badges</span>
  </button>
  <button
    class="bar-btn"
    class:active={settingsOpen}
    onclick={onToggleSettings}
    aria-label="Open settings"
  >
    ⚙ <span class="bar-btn-label">Settings</span>
  </button>
</footer>

<style>
  .bottom-bar {
    display: none;
    min-height: 56px;
    height: auto;
    background: #0d1b2e;
    border-top: 1px solid #1a2e4a;
    align-items: center;
    justify-content: space-around;
    padding: 0 max(4px, env(safe-area-inset-right, 4px)) env(safe-area-inset-bottom, 0px) max(4px, env(safe-area-inset-left, 4px));
    flex-shrink: 0;
  }
  @media (max-width: 1024px) {
    .bottom-bar { display: flex; }
  }
  .bar-btn {
    position: relative;
    display: flex;
    align-items: center;
    gap: 4px;
    min-width: 44px;
    min-height: 44px;
    padding: 0 10px;
    background: transparent;
    border: 1px solid transparent;
    border-radius: 8px;
    color: #4a6080;
    font-size: 13px;
    cursor: pointer;
    transition: all 0.15s;
    touch-action: manipulation;
  }
  .bar-btn:hover, .bar-btn.active {
    color: #4af0c0;
    border-color: #4af0c040;
    background: #0f2035;
  }
  .bar-btn-label {
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
</style>
```
  </action>
</task>

</tasks>

<verification>
- TopBar has badge-pulse animation class applied reactively on earnedAchievements size increase
- BottomBar has 4 buttons, no .bottom-stat, new achievementsOpen + onToggleAchievements props
- `npm run build` exits 0 (note: +page.svelte will have TypeScript error until Plan 07 wires the new props — acceptable as a mid-phase state)
</verification>
