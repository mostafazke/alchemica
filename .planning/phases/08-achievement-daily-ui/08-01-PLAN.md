---
phase: 08-achievement-daily-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/data/badges.ts
  - src/lib/stores/settings.ts
  - src/lib/stores/toast.ts
autonomous: true
requirements:
  - PROG-04
  - PROG-05
  - PROG-06

must_haves:
  truths:
    - "BADGES constant is exported from data/badges.ts with 4 entries: badge_10, badge_25, badge_50, badge_61"
    - "Each badge has: id (AchievementId), emoji, name, threshold (number)"
    - "soundMuted store persists to localStorage key 'alchemica_settings' as { soundMuted: boolean }"
    - "soundMuted is initialized from localStorage on module load (survives reload)"
    - "toastQueue is a writable<AchievementId[]> store, initially []"
    - "All three files have NO circular imports — only from svelte/store and ../types.js"
  artifacts:
    - path: "src/lib/data/badges.ts"
      provides: "BADGES constant + BadgeInfo type"
      exports: ["BADGES", "BadgeInfo"]
    - path: "src/lib/stores/settings.ts"
      provides: "soundMuted writable store with localStorage persistence"
      exports: ["soundMuted"]
    - path: "src/lib/stores/toast.ts"
      provides: "toastQueue writable store for achievement toast pipeline"
      exports: ["toastQueue"]
---

<objective>
Create three new data/store foundation files for Phase 8 UI.

**File 1:** `src/lib/data/badges.ts` — badge metadata constant shared by Gallery and Toast.
**File 2:** `src/lib/stores/settings.ts` — `soundMuted` writable + localStorage persistence.
**File 3:** `src/lib/stores/toast.ts` — `toastQueue` writable for the achievement toast pipeline.

These are the leaf-level files with no component dependencies. All Wave 2 components import from these.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/data/badges.ts</name>
  <files>src/lib/data/badges.ts</files>
  <action>
Create this file with exactly this content:

```ts
import type { AchievementId } from '../types.js';

export interface BadgeInfo {
  id: AchievementId;
  emoji: string;
  name: string;
  threshold: number;
}

export const BADGES: BadgeInfo[] = [
  { id: 'badge_10', emoji: '🔬', name: 'Apprentice',   threshold: 10 },
  { id: 'badge_25', emoji: '⚗️',  name: 'Alchemist',   threshold: 25 },
  { id: 'badge_50', emoji: '🔮', name: 'Sage',         threshold: 50 },
  { id: 'badge_61', emoji: '✨', name: 'Grand Master', threshold: 61 },
];
```
  </action>
</task>

<task type="auto">
  <name>Task 2: Create src/lib/stores/settings.ts</name>
  <files>src/lib/stores/settings.ts</files>
  <action>
Create this file with exactly this content:

```ts
/**
 * settings.ts — App settings store.
 * soundMuted persists to localStorage key 'alchemica_settings'.
 * Per D-09, PROG-06.
 */
import { writable } from 'svelte/store';

const SETTINGS_KEY = 'alchemica_settings';

function loadSoundMuted(): boolean {
  try {
    const raw = localStorage.getItem(SETTINGS_KEY);
    if (!raw) return false;
    const parsed = JSON.parse(raw) as { soundMuted?: boolean };
    return parsed.soundMuted === true;
  } catch {
    return false;
  }
}

export const soundMuted = writable<boolean>(
  typeof localStorage !== 'undefined' ? loadSoundMuted() : false
);

soundMuted.subscribe((muted) => {
  try {
    localStorage.setItem(SETTINGS_KEY, JSON.stringify({ soundMuted: muted }));
  } catch {
    // localStorage unavailable — fail silently
  }
});
```
  </action>
</task>

<task type="auto">
  <name>Task 3: Create src/lib/stores/toast.ts</name>
  <files>src/lib/stores/toast.ts</files>
  <action>
Create this file with exactly this content:

```ts
/**
 * toast.ts — Achievement toast queue store.
 * MixingChamber pushes AchievementId values here.
 * AchievementToast.svelte drains them one at a time.
 * Per D-04, D-11, PROG-05.
 */
import { writable } from 'svelte/store';
import type { AchievementId } from '../types.js';

/** FIFO queue of badge IDs waiting to be shown as toasts. */
export const toastQueue = writable<AchievementId[]>([]);
```
  </action>
</task>

</tasks>

<verification>
- All 3 files created with no TypeScript errors
- `npm run build` exits 0
- No circular imports (data/badges.ts ← types.ts only; stores/*.ts ← svelte/store + types.ts only)
</verification>
