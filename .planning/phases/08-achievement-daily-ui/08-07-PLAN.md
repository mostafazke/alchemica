---
phase: 08-achievement-daily-ui
plan: 07
type: execute
wave: 3
depends_on: [08-02, 08-03, 08-04, 08-05, 08-06]
files_modified:
  - src/routes/+page.svelte
autonomous: true
requirements:
  - PROG-03
  - PROG-04
  - PROG-05
  - DALY-01
  - DALY-05
  - STRK-01

must_haves:
  truths:
    - "AchievementGallery is mounted in +page.svelte with achievementsOpen state"
    - "AchievementToast is mounted once — self-manages from toastQueue store"
    - "DailyChallenge is rendered inside .lab-wrapper center column below MixingChamber"
    - "BottomBar receives achievementsOpen + onToggleAchievements props"
    - "No layout regressions — DailyChallenge renders inside the center grid area"
  artifacts:
    - path: "src/routes/+page.svelte"
      provides: "Full wiring of all Phase 8 components"
---

<objective>
Wire all Phase 8 components into `src/routes/+page.svelte`:

1. Import 3 new components: `AchievementGallery`, `AchievementToast`, `DailyChallenge`
2. Add state: `let achievementsOpen = $state(false)`
3. Wrap MixingChamber + DailyChallenge in a flex column container inside the center grid cell
4. Add `achievementsOpen` + `onToggleAchievements` to `<BottomBar>`
5. Mount `<AchievementGallery>` after `<SettingsPanel>`
6. Mount `<AchievementToast />` at app root level (self-managing, no props needed)

**Center column layout:** The mixing chamber grid cell needs MixingChamber stacked above DailyChallenge.
Use a wrapper `<div class="center-col">` with `display: flex; flex-direction: column; overflow: hidden;` inside the grid area.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Modify src/routes/+page.svelte</name>
  <files>src/routes/+page.svelte</files>
  <action>
Replace the ENTIRE `<script>` block and template (up to `<style>`) with the following. Keep the existing `<style>` block unchanged — only append new rules at the end.

**New script block:**
```svelte
<script lang="ts">
	import TopBar from '$lib/components/TopBar.svelte';
	import Shelf from '$lib/components/Shelf.svelte';
	import MixingChamber from '$lib/components/MixingChamber.svelte';
	import DiscoveryLog from '$lib/components/DiscoveryLog.svelte';
	import BottomBar from '$lib/components/BottomBar.svelte';
	import BottomSheet from '$lib/components/BottomSheet.svelte';
	import SettingsPanel from '$lib/components/SettingsPanel.svelte';
	import AchievementGallery from '$lib/components/AchievementGallery.svelte';
	import AchievementToast from '$lib/components/AchievementToast.svelte';
	import DailyChallenge from '$lib/components/DailyChallenge.svelte';

	let shelfOpen = $state(false);
	let discoverySheetOpen = $state(false);
	let settingsOpen = $state(false);
	let achievementsOpen = $state(false);
</script>
```

**New template (replace existing `<div class="app">` ... up to closing `</div>` before `<style>`):**
```html
<div class="app">
	<TopBar />
	<div class="lab-wrapper">
		<div class="shelf-overlay" class:open={shelfOpen} onclick={() => shelfOpen = false} role="none"></div>
		<div class="shelf-container" class:open={shelfOpen}>
			<Shelf />
		</div>
		<div class="center-col">
			<MixingChamber />
			<DailyChallenge />
		</div>
		<div class="discoveries-container">
			<DiscoveryLog />
		</div>
	</div>
	<BottomBar
		{shelfOpen}
		onToggleShelf={() => shelfOpen = !shelfOpen}
		{discoverySheetOpen}
		onToggleDiscoveries={() => discoverySheetOpen = !discoverySheetOpen}
		{achievementsOpen}
		onToggleAchievements={() => achievementsOpen = !achievementsOpen}
		{settingsOpen}
		onToggleSettings={() => settingsOpen = !settingsOpen}
	/>
	<BottomSheet open={discoverySheetOpen} onClose={() => discoverySheetOpen = false}>
		<DiscoveryLog />
	</BottomSheet>
	<SettingsPanel open={settingsOpen} onClose={() => settingsOpen = false} />
	<AchievementGallery open={achievementsOpen} onClose={() => achievementsOpen = false} />
	<AchievementToast />
</div>
```

**Add at the END of the existing `<style>` block** (after all existing rules):
```css
	.center-col {
		grid-area: chamber;
		display: flex;
		flex-direction: column;
		overflow: hidden;
		min-height: 0;
		align-items: center;
	}
```

Note: The existing `.lab-wrapper` CSS uses `grid-template-areas: "shelf chamber discoveries"` — the new `.center-col` takes over the `chamber` grid area (previously `<MixingChamber />` was placed there directly). No other layout rules need changing.
  </action>
</task>

</tasks>

<verification>
- All 3 new components mounted correctly
- BottomBar receives all 4 pairs of open/toggle props
- DailyChallenge visible in center column below MixingChamber
- AchievementGallery opens/closes via BottomBar Badges button
- AchievementToast mounts once at root (no props)
- `npm run build` exits 0 — this is the final integration build gate
</verification>
