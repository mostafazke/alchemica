---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/lib/stores/game.ts
autonomous: true
requirements: [ARCH-03, ARCH-04, GAME-07]
must_haves:
  truths:
    - "game.ts exports unlockedElements, discoveries, slots, combo, score as Svelte writable stores"
    - "Game state auto-saves to localStorage on every change"
    - "Save format is versioned: { version: number, data: GameState }"
    - "Initial state loads from localStorage if available, falls back to defaults"
  artifacts:
    - src/lib/stores/game.ts
  key_links:
    - "game.ts imports GameState, Discovery, Slots from lib/types.ts"
    - "game.ts imports BASIC_ELEMENTS from lib/data/elements.ts for initial unlocked set"
---

<objective>
Build Svelte 5 reactive stores for all game state with auto-save to localStorage.
The save format is versioned so future schema changes can migrate without data loss.

Purpose: Central reactive state that components subscribe to — replaces manual DOM diffing.
Output: src/lib/stores/game.ts with 5 exported stores and auto-save.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/stores/game.ts with Svelte stores</name>
  <files>src/lib/stores/game.ts</files>
  <read_first>src/lib/types.ts, src/lib/data/elements.ts</read_first>
  <action>
    Create src/lib/stores/game.ts. Use Svelte's `writable` store (NOT runes — stores are
    compatible with Svelte 5 and are the correct approach for cross-component shared state).

    ```typescript
    import { writable, get } from 'svelte/store';
    import type { Discovery, Slots } from '../types.js';
    import { BASIC_ELEMENTS } from '../data/elements.js';

    const SAVE_KEY = 'alchemica_v1';
    const SAVE_VERSION = 1;

    // --- Load from localStorage ---
    function loadSave() {
      try {
        const raw = localStorage.getItem(SAVE_KEY);
        if (!raw) return null;
        const parsed = JSON.parse(raw);
        if (parsed.version !== SAVE_VERSION) return null; // version mismatch — start fresh
        return parsed.data;
      } catch {
        return null;
      }
    }

    const saved = loadSave();

    // --- Stores ---
    export const unlockedElements = writable<Set<string>>(
      saved?.unlockedElements ? new Set(saved.unlockedElements) : new Set(BASIC_ELEMENTS)
    );

    export const discoveries = writable<Discovery[]>(saved?.discoveries ?? []);

    export const slots = writable<Slots>({ a: null, b: null });

    export const combo = writable<number>(1);

    export const score = writable<number>(saved?.score ?? 0);

    export const lastSuccess = writable<boolean>(false);

    // --- Auto-save on change ---
    function saveToStorage() {
      try {
        const state = {
          version: SAVE_VERSION,
          data: {
            unlockedElements: [...get(unlockedElements)],
            discoveries: get(discoveries),
            score: get(score),
          }
        };
        localStorage.setItem(SAVE_KEY, JSON.stringify(state));
      } catch {
        // localStorage unavailable (private browsing, quota exceeded) — fail silently
      }
    }

    unlockedElements.subscribe(saveToStorage);
    discoveries.subscribe(saveToStorage);
    score.subscribe(saveToStorage);
    ```

    Also create a `resetGame()` export that clears localStorage and resets all stores to defaults.
  </action>
  <verify>test -f src/lib/stores/game.ts && grep -q 'writable' src/lib/stores/game.ts</verify>
  <done>game.ts exports writable stores with auto-save</done>
</task>

</tasks>
