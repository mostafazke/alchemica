---
phase: 14-landscape-game-ux
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/lib/components/Shelf.svelte
  - src/routes/game/+page.svelte
autonomous: false
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "Shelf.svelte is deleted — no references remain anywhere"
    - "svelte-check passes with zero errors"
    - "Production build succeeds (npm run build)"
    - "All game features work: element selection, reactions, achievements, daily challenge, hints, sounds, particles"
    - "Capacitor sync succeeds (npx cap sync android)"
  artifacts:
    - path: "src/lib/components/ElementGrid.svelte"
      provides: "Replaced Shelf — must exist"
    - path: "src/routes/game/+page.svelte"
      provides: "No Shelf imports"
  key_links:
    - from: "src/routes/game/+page.svelte"
      to: "src/lib/components/ElementGrid.svelte"
      via: "import ElementGrid"
      pattern: "import ElementGrid"
---

<objective>
Delete Shelf.svelte, clean up all stale references, verify full build pipeline and game functionality.

Purpose: Final integration pass — remove the old Shelf component (now replaced by ElementGrid from Plan 01), verify no broken imports remain, and ensure the complete build chain works (TypeScript check → production build → native sync).

Output: Clean codebase with no dead code, passing builds, verified game functionality.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-landscape-game-ux/14-CONTEXT.md
@src/routes/game/+page.svelte (should already have ElementGrid from Plan 01; verify no Shelf import)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Shelf.svelte and clean all references</name>
  <files>src/lib/components/Shelf.svelte, src/routes/game/+page.svelte</files>
  <action>
**Delete the file:**
```bash
rm src/lib/components/Shelf.svelte
```

**Search for any remaining references:**
```bash
grep -rn "Shelf" src/ --include="*.svelte" --include="*.ts"
```

If any stale `import Shelf` or `<Shelf` references found:
- Remove the import line
- Remove any `<Shelf />` or `<Shelf>` usage in templates
- Remove any Shelf-related state variables (e.g., `shelfOpen`) if still present

The game page (`+page.svelte`) should already use ElementGrid from Plan 01 — verify and fix if needed.

Also check BottomBar no longer references `shelfOpen` or `onToggleShelf` (should be cleaned in Plan 03).
  </action>
  <verify>
    <automated>grep -rn "Shelf" src/ --include="*.svelte" --include="*.ts" | grep -v "node_modules" | wc -l | grep -q "^0$" && echo "PASS: no Shelf references" || echo "FAIL: stale Shelf references found"</automated>
  </verify>
  <done>Shelf.svelte deleted, zero references to "Shelf" remain in src/</done>
</task>

<task type="auto">
  <name>Task 2: Build verification pipeline</name>
  <files></files>
  <action>
Run the full verification pipeline in sequence:

1. **TypeScript check:**
```bash
npx svelte-check --threshold error
```
Fix any type errors (likely from removed Shelf props in BottomBar if not already fixed).

2. **Production build:**
```bash
npm run build
```
Must complete with exit code 0.

3. **Capacitor sync:**
```bash
npx cap sync android
```
Must complete successfully (copies web assets to android project).

If any step fails, diagnose and fix the issue before proceeding to the next step.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>svelte-check passes, production build succeeds, cap sync android completes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete landscape game UX rewrite:
- Left panel: scrollable element grid with filter tabs (All/Basic/Found)
- Right panel: mixing workspace (slots → react → result → hint+daily utility row)
- Bottom bar: 3 overlay tabs (Discoveries, Badges, Settings)
- Portrait rotation overlay
- All old portrait breakpoints removed
  </what-built>
  <how-to-verify>
1. Run `npm run dev -- --host` and open on device or emulator
2. **Layout check:** Verify 2-panel landscape layout — element grid on left (~55%), mixing workspace on right (~45%)
3. **Element grid:** All unlocked elements visible as compact tiles in a scrollable grid. Tap filter tabs (All/Basic/Found) — grid filters correctly
4. **Element selection:** Tap an element tile — it fills Slot A. Tap another — fills Slot B. Tap a third — replaces Slot A
5. **React:** With two elements selected, tap "⚗ React" — reaction works, particles fire, result displays
6. **Utility row:** Hint button and Daily Challenge sit side-by-side below the result
7. **Bottom bar:** 3 tabs visible — Discoveries, Badges, Settings. Each opens its overlay
8. **Portrait overlay:** Rotate device to portrait — "rotate your device" message appears
9. **Sounds/haptics:** Verify sounds play on reactions (if unmuted), haptics work on element selection
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | T (Tampering) | Dead code removal | accept | Removing unused code reduces attack surface |
</threat_model>

<verification>
1. `grep -rn "Shelf" src/` returns zero results
2. `npx svelte-check --threshold error` — zero errors
3. `npm run build` — exit code 0
4. `npx cap sync android` — success
5. Human verification of visual layout and all game features
</verification>

<success_criteria>
- Shelf.svelte deleted with zero stale references
- Full build pipeline passes (check → build → cap sync)
- Human-verified: landscape layout correct, all game features functional
- Phase 14 complete — UX-01, UX-02, UX-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/14-landscape-game-ux/14-04-SUMMARY.md`
</output>
