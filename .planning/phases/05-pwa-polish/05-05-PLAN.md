# Plan 05-05: Verification and STATE.md update

## Goal
Run full quality checks, verify all Phase 5 success criteria are met, and update STATE.md.

## Tasks

### Task 1 — svelte-check
Run `npm run check` → expect 0 errors, 0 warnings.

### Task 2 — Build verification
Run `npm run build` → expect successful build.
Verify presence of: `sw.js`, `registerSW.js`, `manifest.webmanifest`, `icon-192.png`, `icon-512.png`.

### Task 3 — Success criteria checklist
Verify against ROADMAP Phase 5 success criteria:
- [ ] SW files present (offline cacheable)
- [ ] manifest.webmanifest with correct fields
- [ ] Icons at 192px and 512px
- [ ] OfflineIndicator component exists and wired to layout
- [ ] No CDN refs in build
- [ ] apple-touch-icon in app.html

### Task 4 — Update STATE.md
Mark Phase 5 ✓ Complete. Update session log.

### Task 5 — Commit
`git add -A && git commit -m "feat(phase-5): PWA, offline & polish"`
